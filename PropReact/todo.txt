make simple props be structs?
support conditional access ?.
generate builder code, lookup using [CallerArgumentExpression]
enforce only access expressions
enforce static lambdas
enforce passing "this" into Prop.Watch(this, ....)
allow subscribing directly to props _someProp.Subscribe(x => ...);

// todo: start chain not from root but from props -> then generate Dispose() pattern for said props
// Prop.Watch(_someProp, Action/Selector, Action?);
// _someProp.Watch(Selector?)


allow watching without values Prop.Watch(x => x.1.2.3, () => {...}) - avoids allocations for new{} and selectors



GetNextNode() - avoid duplicate nodes when branching based on getter

transaction: StartRead(), StartWritable();

reorder stuff to be consistent
use collectionsmarshal in collections to avoid double hash

conserve chain type information, then construct the getter on Start() in reverse - run "DFS", should avoid Select() issues
type xml documentation

PropConfig.TransactionMode = { Ignore, Validate, Enforce }